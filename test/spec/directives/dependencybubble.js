'use strict';

describe('Directive: dependencybubble', function () {

  // load the directive's module
  beforeEach(module('mohiApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

});
